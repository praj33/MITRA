# ── Core Framework ──
fastapi==0.95.2
uvicorn[standard]==0.20.0
python-multipart==0.0.6
python-dotenv==1.0.0

# ── HTTP / Networking ──
requests==2.31.0
httpx==0.24.1

# ── Database ──
motor==3.1.1
pymongo==4.3.3

# ── LLM Providers ──
groq==0.4.1
openai==1.3.0

# ── Auth & Security ──
python-jose[cryptography]==3.3.0
ecdsa==0.19.1
email-validator==1.3.1

# ── Templating ──
jinja2==3.1.2

# ── Telephony ──
twilio==8.2.0

# ── Voice & Audio (Nilesh — duplex-audio) ──
openai-whisper>=20230918
SpeechRecognition==3.10.4
pydub==0.25.1
gTTS>=2.3.0

# ── Language Detection ──
langdetect==1.0.9
python-dateutil==2.8.2

# ── Session Persistence (optional) ──
redis>=4.5.0

# ── Schema Validation (UniGuru contracts) ──
jsonschema>=4.17.0

# ── Monitoring (optional) ──
sentry-sdk[fastapi]>=1.14.0
